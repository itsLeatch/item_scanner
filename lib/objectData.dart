//here are the image with their ai description and maybe some other fields
class ObjectData {
  final int? id;
  final String? imagePath;
  final String? description;

  ObjectData({this.id, this.imagePath, this.description});

}